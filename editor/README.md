# H5PEditor.ColorSelector (vendored)

Copia local de `H5PEditor.ColorSelector` 1.3.2 con un ajuste de validación:

- Los colores `optional` vacíos pasan `validate()`, para que los grupos del editor (Apariencia, Vista de solución, etc.) se puedan contraer.

Se publica a Lumi con `npm run sync:lumi` (script `scripts/sync-editor-deps.ps1`).
